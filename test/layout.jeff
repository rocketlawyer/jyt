(function(){
    return {
        html: {
            head: {
                title: "I am a page title"
            },
            body: {
                "include": "bodyContent",           // one way to include a template - a named extension point
                "div": include("rightNavigation")   // or put an extension point in this way
            }
        }
    }
}());